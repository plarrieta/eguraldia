name: Euskalmet datuak eguneratu

on:
  workflow_dispatch:
  schedule:
    - cron: "17 6 * * 1"

permissions:
  contents: write

concurrency:
  group: euskalmet-datuak
  cancel-in-progress: false

jobs:
  eguneratu:
    runs-on: ubuntu-latest

    steps:
      - name: Biltegia jaitsi
        uses: actions/checkout@v4

      - name: Python prestatu
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Tenperatura historikoaren diagnostikoa
        run: python tmp/diagnostikoa_tenperatura_osoa.py

      - name: Aldaketak GitHubera igo
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add tmp/tenperatura_anomaliak.json tmp/tenperatura_anomaliak.csv

          if git diff --cached --quiet; then
            echo "Ez dago aldaketarik."
            exit 0
          fi

          git commit -m "Tenperatura historikoaren diagnostikoa eguneratu"
          git push
